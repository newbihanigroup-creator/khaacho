apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: khaacho
  labels:
    app: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: api
  sessionAffinity: None
